import { Beaker, TestTube, Square, Droplet, CircleDot, Flame, HelpCircle } from 'lucide-react';

// Từ điển ánh xạ tên icon từ Database sang component của lucide-react
export const LabIconRegistry = {
  Beaker,
  TestTube,
  Square,
  Droplet,
  CircleDot,
  Flame,
  HelpCircle
};
